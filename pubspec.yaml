name: location_opinions
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.1.3
  firebase_core: ^4.5.0
  firebase_auth: ^6.2.0
  uuid: ^4.5.3
  shared_preferences: ^2.5.4
  flutter_colorpicker: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
